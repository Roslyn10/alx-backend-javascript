// A test file for the calculator, but it uses chai instead of mocha
const { expect } = require('chai');
const { it, describe } = require('mocha');
const calculateNumber = require('./2-calcul_chai');

describe("calculateNumber()", function () {
